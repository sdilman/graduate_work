services:

  app:
    build:
      context: ../../
      dockerfile: Dockerfile
    env_file:
      - .env
    expose:
      - ${APP_PORT}

  tests:
    build:
      context: ../../
      dockerfile: tests/integration/Dockerfile
    env_file:
      - .env
    depends_on:
      - app
