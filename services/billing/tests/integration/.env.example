# Project settings

APP_NAME="Billing"
APP_HOST="localhost"
APP_PORT=8075
APP_ROOT_PATH="/billing"

# API settings
API_VERSION="v1"
API_DOCS_URL="/api/${API_VERSION}/openapi"
API_OPENAPI_URL="/api/${API_VERSION}/openapi.json"

# Postgres settings
PG_DB=billing_database
PG_USER=app
PG_PASSWORD=123qwe
PG_HOST=billing_app_postgres
PG_PORT=5432
PG_ASYNC_SCHEMA=postgresql+asyncpg
PG_DSN=${PG_ASYNC_SCHEMA}://${PG_USER}:${PG_PASSWORD}@${PG_HOST}:${PG_PORT}/${PG_DB}
PG_DSN_LOCAL=${PG_ASYNC_SCHEMA}://${PG_USER}:${PG_PASSWORD}@localhost:${PG_PORT}/${PG_DB}

# JWT token settings
JWT_SECRET_KEY="some-secret-key"
JWT_ALGORITHM=HS256
JWT_AT_EXPIRE_MINUTES=30
JWT_RT_EXPIRE_MINUTES=1440

# Token Cookie
AUTH_ACCESS_NAME="auth-app-access-key"
AUTH_REFRESH_NAME="auth-app-refresh-key"
AUTH_HOST=
AUTH_PORT=
AUTH_VERSION=
AUTH_USER_HANDLE=
AUTH_URL_USER="http://${AUTH_HOST}:${AUTH_PORT}/api/${AUTH_VERSION}/${AUTH_USER_HANDLE}
